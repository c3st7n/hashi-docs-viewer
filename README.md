# hashi-docs-viewer
View old versions of hashicorp docs sites

Current thinking:
 - hourly: check if there is a new version of the docs for each hashicorp repo
   - if new versions found, build them into a versions directory
 - index page lists products and versions available
